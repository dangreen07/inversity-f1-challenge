import React from "react";
import Heading from "../ui/heading";

export default function WeatherPage() {
    return (
        <main className="flex flex-col w-full h-full">
            <Heading pageName="Weather" />
        </main>
    );
}